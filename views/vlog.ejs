<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Casalinga Vlog</title>
    <link rel="stylesheet" href="/css/style.css"> 
    
    <style>
        :root {
            --bg-color: #f4f4f5;
            --card-bg: #ffffff;
            --text-primary: #27272a;
            --text-secondary: #71717a;
            --accent: #52525b;
            --border-radius: 8px;
        }

        body {
            margin: 0;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            display: flex;
            justify-content: center;
            min-height: 100vh;
        }

        .main-container {
            width: 100%;
            max-width: 900px;
            margin-top: 40px;
            padding: 20px;
        }

        .video-wrapper {
            background-color: #000;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        video {
            width: 100%;
            height: auto;
            display: block;
            max-height: 80vh;
        }

        .video-info {
            background-color: var(--card-bg);
            padding: 24px;
            margin-top: 16px;
            border-radius: var(--border-radius);
            border: 1px solid #e4e4e7;
        }

        .video-title { margin: 0 0 12px 0; font-size: 1.5rem; font-weight: 600; }
        .video-description { color: var(--text-secondary); line-height: 1.6; margin-bottom: 24px; }
        .actions { display: flex; gap: 12px; }
        
        .btn-neutral {
            background-color: #e4e4e7;
            color: var(--text-primary);
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            cursor: pointer;
            font-weight: 500;
        }
        .btn-neutral:hover { background-color: #d4d4d8; }
    </style>
</head>
<body>

    <main class="main-container">
        <div class="video-wrapper">
            <video id="mainVideo" controls>
                <source src="/videos/my-tour.mp4" type="video/mp4">
                Your browser does not support the video tag.
            </video>
        </div>

        <div class="video-info">
            <h1 class="video-title">Welcome to <strong>Casalinga</strong>!</h1>
            <p class="video-description">
                We are thrilled to have you here. Casalinga is more than just a platform; it's a community dedicated to exploring the beauty of home and nature.
            </p>
            
            <div class="actions">
                <button class="btn-neutral">Share</button>
                <button class="btn-neutral">Download</button>
            </div>
        </div>
    </main>

</body>
</html>