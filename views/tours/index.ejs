<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,100..1000&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet">

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                container: { center: true, padding: "2rem", screens: { "2xl": "1400px" } },
                extend: {
                    fontFamily: { sans: ['DM Sans', 'sans-serif'], serif: ['Playfair Display', 'serif'] },
                    colors: {
                        background: "#faf8f5",
                        foreground: "oklch(0.145 0 0)",
                        primary: { DEFAULT: "#1e6b38", foreground: "#ffffff" },
                        secondary: { DEFAULT: "#f2eadd", foreground: "#1a1a1a" },
                        card: { DEFAULT: "#ffffff", foreground: "#000000" },
                        muted: { DEFAULT: "#f3f4f6", foreground: "#6b7280" },
                        border: "#e5e7eb"
                    }
                }
            }
        }
    </script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        .reveal { opacity: 0; transform: translateY(20px); transition: all 0.8s ease-out; }
        .reveal.active { opacity: 1; transform: translateY(0); }
        .tour-card:hover .tour-image { transform: scale(1.1); }
    </style>
</head>
<body class="font-sans antialiased bg-background text-foreground overflow-x-hidden">

    <header id="main-header" class="fixed top-0 left-0 right-0 z-50 transition-all duration-300 bg-white/95 backdrop-blur-md shadow-sm py-3">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 flex items-center justify-between">
            <a href="/" class="flex items-center gap-2 group">
                <i data-lucide="mountain" class="h-8 w-8 text-primary"></i>
                <span class="font-serif text-2xl font-semibold text-gray-900">Casalinga</span>
            </a>
            
            <nav class="hidden md:flex items-center gap-1">
                <a href="/" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary rounded-full transition-colors">Home</a>
                <a href="/tours" class="px-4 py-2 text-sm font-medium text-primary bg-primary/10 rounded-full transition-colors">Tours</a>
                <a href="/about" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary rounded-full transition-colors">About</a>
                <a href="/contact" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary rounded-full transition-colors">Contact</a>
            </nav>

            <div class="hidden md:flex items-center gap-3">
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/user/dashboard" class="px-6 py-2 rounded-full bg-primary text-white text-sm font-medium hover:bg-primary/90 transition-all shadow-md">Dashboard</a>
                <% } else { %>
                    <a href="/auth/login" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary">Login</a>
                    <a href="/auth/register" class="px-6 py-2 rounded-full bg-primary text-white text-sm font-medium hover:bg-primary/90 transition-all shadow-md">Sign Up</a>
                <% } %>
            </div>
             <button id="mobile-menu-btn" class="md:hidden p-2 rounded-lg text-gray-600">
                <i data-lucide="menu" class="h-6 w-6"></i>
            </button>
        </div>
        
        <div id="mobile-menu" class="md:hidden hidden bg-white border-t border-gray-100">
            <div class="p-4 space-y-2">
                <a href="/" class="block px-4 py-2 text-gray-600 hover:bg-gray-50 rounded-lg">Home</a>
                <a href="/tours" class="block px-4 py-2 text-primary bg-primary/5 rounded-lg">Tours</a>
                <a href="/about" class="block px-4 py-2 text-gray-600 hover:bg-gray-50 rounded-lg">About</a>
                <a href="/contact" class="block px-4 py-2 text-gray-600 hover:bg-gray-50 rounded-lg">Contact</a>
            </div>
        </div>
    </header>

    <section class="relative pt-32 pb-16 bg-primary/5">
        <div class="mx-auto max-w-7xl px-4 text-center">
            <span class="inline-block text-primary text-sm font-semibold tracking-wider uppercase mb-4 reveal">Discover Your Next Journey</span>
            <h1 class="font-serif text-4xl md:text-6xl font-bold mb-6 text-gray-900 reveal" style="transition-delay: 100ms;">Explore Our Tours</h1>
            <p class="text-gray-600 max-w-2xl mx-auto mb-10 text-lg reveal" style="transition-delay: 200ms;">From the mountains of Mpumalanga to the coast of the Garden Route.</p>
            
            <div class="max-w-3xl mx-auto reveal" style="transition-delay: 300ms;">
                <form action="/tours" method="GET" class="bg-white p-2 rounded-full shadow-lg border border-gray-100 flex flex-col md:flex-row gap-2">
                    <div class="flex-1 relative">
                        <i data-lucide="search" class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400"></i>
                        <input type="text" name="search" value="<%= filters.search || '' %>" placeholder="Search destinations or experiences..." class="w-full pl-12 pr-4 py-3 rounded-full border-none focus:ring-0 text-gray-800 placeholder:text-gray-400 bg-transparent">
                    </div>
                    <div class="h-px md:h-auto md:w-px bg-gray-200 my-2 md:my-0"></div>
                    <div class="w-full md:w-48 relative">
                         <i data-lucide="calendar" class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400"></i>
                        <input type="date" name="date" value="<%= filters.date || '' %>" class="w-full pl-12 pr-4 py-3 rounded-full border-none focus:ring-0 text-gray-800 bg-transparent text-sm">
                    </div>
                    <button type="submit" class="bg-primary hover:bg-primary/90 text-white px-8 py-3 rounded-full font-medium transition-all shadow-md">
                        Search
                    </button>
                </form>
            </div>
        </div>
    </section>

    <section class="py-16 bg-background">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            
            <div class="flex flex-wrap justify-center gap-3 mb-12 reveal">
                <a href="/tours" 
                   class="px-6 py-2.5 rounded-full text-sm font-medium transition-all duration-300 <%= !filters.category ? 'bg-primary text-white shadow-lg shadow-primary/25' : 'bg-white border border-gray-200 text-gray-600 hover:border-primary hover:text-primary' %>">
                   All Tours
                </a>
                <% categories.forEach(cat => { %>
                    <a href="/tours?category=<%= cat.name %>" 
                       class="px-6 py-2.5 rounded-full text-sm font-medium transition-all duration-300 <%= filters.category === cat.name ? 'bg-primary text-white shadow-lg shadow-primary/25' : 'bg-white border border-gray-200 text-gray-600 hover:border-primary hover:text-primary' %>">
                        <%= cat.name %>
                    </a>
                <% }) %>
            </div>

            <% if (tours && tours.length > 0) { %>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    <% tours.forEach((tour, index) => { %>
                        <div class="group relative bg-white rounded-3xl overflow-hidden shadow-sm hover:shadow-xl transition-all duration-500 hover:-translate-y-1 border border-gray-100 reveal tour-card" style="transition-delay: <%= index * 50 %>ms;">
                            
                            <div class="relative h-72 overflow-hidden">
                                <img src="<%= tour.main_image || '/images/placeholder-tour.jpg' %>" alt="<%= tour.title %>" class="w-full h-full object-cover transition-transform duration-700 tour-image">
                                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-60"></div>
                                
                                <div class="absolute top-4 left-4 flex flex-wrap gap-2">
                                    <% if (tour.featured) { %>
                                        <span class="px-3 py-1 rounded-full bg-amber-400 text-amber-950 text-xs font-bold shadow-sm">Popular</span>
                                    <% } %>
                                    <% if (tour.discount_price) { %>
                                        <span class="px-3 py-1 rounded-full bg-red-500 text-white text-xs font-bold shadow-sm">Sale</span>
                                    <% } %>
                                </div>

                                <button class="absolute top-4 right-4 p-2.5 rounded-full bg-white/20 backdrop-blur-md hover:bg-white transition-colors group/heart">
                                    <i data-lucide="heart" class="w-5 h-5 text-white group-hover/heart:text-red-500 transition-colors"></i>
                                </button>

                                <div class="absolute bottom-4 right-4 bg-white/95 backdrop-blur-sm px-4 py-2 rounded-full shadow-lg border border-white/50">
                                    <div class="flex items-baseline gap-1">
                                        <span class="text-xs text-gray-500">From</span>
                                        <% if (tour.discount_price) { %>
                                            <span class="text-gray-400 line-through text-xs">R<%= parseFloat(tour.price).toLocaleString() %></span>
                                            <span class="text-primary font-bold">R<%= parseFloat(tour.discount_price).toLocaleString() %></span>
                                        <% } else { %>
                                            <span class="text-primary font-bold">R<%= parseFloat(tour.price).toLocaleString() %></span>
                                        <% } %>
                                    </div>
                                </div>
                            </div>

                            <div class="p-6">
                                <div class="flex items-center gap-4 text-xs text-gray-500 mb-4 font-medium">
                                    <div class="flex items-center gap-1.5 bg-gray-50 px-2 py-1 rounded-md">
                                        <i data-lucide="clock" class="w-3.5 h-3.5 text-primary"></i>
                                        <%= tour.duration_days %> Days
                                    </div>
                                    <div class="flex items-center gap-1.5 bg-gray-50 px-2 py-1 rounded-md">
                                        <i data-lucide="users" class="w-3.5 h-3.5 text-primary"></i>
                                        <%= tour.capacity %> Spots
                                    </div>
                                    <div class="flex items-center gap-1.5 bg-gray-50 px-2 py-1 rounded-md ml-auto">
                                        <i data-lucide="star" class="w-3.5 h-3.5 text-amber-400 fill-amber-400"></i>
                                        4.9
                                    </div>
                                </div>

                                <h3 class="font-serif text-xl font-bold text-gray-900 mb-2 leading-tight group-hover:text-primary transition-colors">
                                    <a href="/tours/<%= tour.slug %>">
                                        <%= tour.title %>
                                    </a>
                                </h3>
                                
                                <p class="text-gray-600 text-sm leading-relaxed line-clamp-2 mb-6 h-10">
                                    <%= tour.short_description %>
                                </p>

                                <div class="flex items-center justify-between pt-4 border-t border-gray-100">
                                    <div class="flex items-center gap-1.5 text-sm text-gray-500">
                                        <i data-lucide="map-pin" class="w-4 h-4 text-gray-400"></i>
                                        <%= tour.location %>
                                    </div>
                                    
                                    <a href="/tours/<%= tour.slug %>" class="inline-flex items-center gap-2 text-sm font-bold text-primary hover:gap-3 transition-all">
                                        View Details <i data-lucide="arrow-right" class="w-4 h-4"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="text-center py-24 bg-white rounded-3xl border border-dashed border-gray-200 reveal">
                    <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-gray-50 mb-4">
                        <i data-lucide="search-x" class="w-8 h-8 text-gray-400"></i>
                    </div>
                    <h3 class="font-serif text-xl font-bold text-gray-900 mb-2">No tours found</h3>
                    <p class="text-gray-500 max-w-md mx-auto mb-6">We couldn't find any tours matching your criteria. Try adjusting your filters or search terms.</p>
                    <a href="/tours" class="inline-flex items-center justify-center px-6 py-2 rounded-full border border-gray-300 text-gray-700 font-medium hover:bg-gray-50 transition-colors">
                        Clear Filters
                    </a>
                </div>
            <% } %>

            <% if (typeof totalPages !== 'undefined' && totalPages > 1) { %>
                <div class="mt-16 flex justify-center gap-2 reveal">
                    <% if (currentPage > 1) { %>
                        <a href="?page=<%= currentPage - 1 %>&category=<%= filters.category || '' %>&search=<%= filters.search || '' %>" class="w-10 h-10 flex items-center justify-center rounded-full border border-gray-200 text-gray-600 hover:border-primary hover:text-primary hover:bg-primary/5 transition-all">
                            <i data-lucide="chevron-left" class="w-5 h-5"></i>
                        </a>
                    <% } %>

                    <% for(let i = 1; i <= totalPages; i++) { %>
                        <a href="?page=<%= i %>&category=<%= filters.category || '' %>&search=<%= filters.search || '' %>" 
                           class="w-10 h-10 flex items-center justify-center rounded-full text-sm font-bold transition-all <%= currentPage === i ? 'bg-primary text-white shadow-lg shadow-primary/25' : 'border border-gray-200 text-gray-600 hover:border-primary hover:text-primary hover:bg-primary/5' %>">
                            <%= i %>
                        </a>
                    <% } %>

                    <% if (currentPage < totalPages) { %>
                        <a href="?page=<%= currentPage + 1 %>&category=<%= filters.category || '' %>&search=<%= filters.search || '' %>" class="w-10 h-10 flex items-center justify-center rounded-full border border-gray-200 text-gray-600 hover:border-primary hover:text-primary hover:bg-primary/5 transition-all">
                            <i data-lucide="chevron-right" class="w-5 h-5"></i>
                        </a>
                    <% } %>
                </div>
            <% } %>

        </div>
    </section>

    <footer class="bg-gray-900 text-white pt-20 pb-10">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-1 md:grid-cols-4 gap-12 mb-16 border-b border-white/10 pb-12">
                <div class="col-span-1 md:col-span-2">
                    <a href="/" class="flex items-center gap-2 mb-6">
                        <i data-lucide="mountain" class="h-8 w-8 text-primary"></i>
                        <span class="font-serif text-2xl font-semibold">Casalinga</span>
                    </a>
                    <p class="text-gray-400 leading-relaxed max-w-sm mb-6">
                        Discover wellness through travel, adventure, and connection with nature. Creating transformative experiences since 2010.
                    </p>
                </div>
                <div>
                    <h4 class="font-bold text-lg mb-6">Quick Links</h4>
                    <ul class="space-y-4 text-gray-400">
                        <li><a href="/tours" class="hover:text-primary transition-colors">All Tours</a></li>
                        <li><a href="/about" class="hover:text-primary transition-colors">About Us</a></li>
                        <li><a href="/contact" class="hover:text-primary transition-colors">Contact</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="font-bold text-lg mb-6">Contact</h4>
                    <ul class="space-y-4 text-gray-400">
                        <li class="flex items-center gap-3"><i data-lucide="mail" class="w-4 h-4 text-primary"></i> hello@casalinga.co.za</li>
                        <li class="flex items-center gap-3"><i data-lucide="phone" class="w-4 h-4 text-primary"></i> +27 12 345 6789</li>
                    </ul>
                </div>
            </div>
            <div class="flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-gray-500">
                <p>&copy; 2026 Casalinga Tours. All rights reserved.</p>
                <div class="flex gap-6">
                    <a href="/privacy" class="hover:text-white transition-colors">Privacy Policy</a>
                    <a href="/terms" class="hover:text-white transition-colors">Terms of Service</a>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Initialize Icons
        lucide.createIcons();

        // Reveal Animation Observer
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                }
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

        // Mobile Menu Toggle
        const btn = document.getElementById('mobile-menu-btn');
        const menu = document.getElementById('mobile-menu');

        btn.addEventListener('click', () => {
            menu.classList.toggle('hidden');
        });
    </script>
</body>
</html>